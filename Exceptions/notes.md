# try -- try block is used to test a block of code for errors
# try -- It separates your "risky" code from your error-handling logic, making the code easier to read and maintain.
# try -- It is almost always paired with an except block, which contains the code that runs if an error occurs during the try phase
# ValueError -- specific type of exception raised when a function receives an argument of the correct data type but an inappropriate value
# ValueError -- 