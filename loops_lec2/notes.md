# Shifting to "for" for loops from "while" with integration of term "range"    for extreme cases
# Using while with boolean value(True/False)
# list is set of multiple value
# dict associate something with something else
# Using the term break
# Using list with loops and "len" function 
# range is introduced because list can't scale very well by itself 
# "len" function work it to tell the user how long is the list 